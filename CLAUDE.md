# CLAUDE.md

This file provides guidance to Claude Code (claude.ai/code) when working with code in this repository.

## Project Overview

VoyageVista is a travel-related web application. The repository is in early development with the following planned structure:

```
voyagevista/
├── frontend/    # Client-side application
├── backend/     # Server-side API
├── database/    # Schema, migrations, seed data
└── docs/        # Documentation
```

## Commands

No build, lint, or test commands are defined yet. Update this file once the tech stack is chosen and tooling is set up.

## Architecture

The project is split into three layers — frontend, backend, and database — intended to be developed independently. Once each layer has code, document how they connect here (API contracts, authentication flow, environment variables, etc.).
